    import XCTest
    @testable import DualAuthiOS

    final class DualAuthiOSTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(DualAuthiOS().text, "Hello, World!")
        }
    }
